import LeapYears from "@/components/LeapYears";

export const metadata = {
  title: "About Leap Years | Date & Time Calculator",
  description: "Understanding leap years, their rules, and how to check if a year is a leap year.",
};

export default function LeapYearsPage() {
  return <LeapYears />;
}
