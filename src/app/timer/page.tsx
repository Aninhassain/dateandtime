import Timer from "@/components/Timer";

export const metadata = {
  title: "Timer | Date & Time Calculator",
  description: "Set a countdown timer with customizable duration and alerts. Simple and easy to use online timer.",
};

export default function TimerPage() {
  return <Timer />;
}
