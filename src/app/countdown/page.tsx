import CountdownToDate from "@/components/CountdownToDate";

export const metadata = {
  title: "Countdown to Any Date | Date & Time Calculator",
  description: "Create a countdown timer to any future date and time. Customizable display formats and event titles.",
};

export default function CountdownPage() {
  return <CountdownToDate />;
}
