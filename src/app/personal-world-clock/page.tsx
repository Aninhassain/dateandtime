import PersonalWorldClock from "@/components/PersonalWorldClock";

export const metadata = {
  title: "Personal World Clock | Date & Time Calculator",
  description: "Create your own customized world clock with your favorite cities.",
};

export default function PersonalWorldClockPage() {
  return <PersonalWorldClock />;
}
