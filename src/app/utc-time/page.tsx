import UTCTime from "@/components/UTCTime";

export const metadata = {
  title: "UTC Time | Date & Time Calculator",
  description: "Current Coordinated Universal Time (UTC) with detailed components and information.",
};

export default function UTCTimePage() {
  return <UTCTime />;
}
