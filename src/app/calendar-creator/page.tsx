import CalendarCreator from "@/components/CalendarCreator";

export const metadata = {
  title: "Calendar Creator | Date & Time Calculator",
  description: "Design custom calendars with your preferences. Choose themes and styles.",
};

export default function CalendarCreatorPage() {
  return <CalendarCreator />;
}
