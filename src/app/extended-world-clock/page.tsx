import ExtendedWorldClock from "@/components/ExtendedWorldClock";

export const metadata = {
  title: "Extended World Clock | Date & Time Calculator",
  description: "Comprehensive world clock with cities across all continents and time zones.",
};

export default function ExtendedWorldClockPage() {
  return <ExtendedWorldClock />;
}
