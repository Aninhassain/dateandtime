import PrintableCalendar from "@/components/PrintableCalendar";

export const metadata = {
  title: "Printable Calendar | Date & Time Calculator",
  description: "Generate and print calendars for any year. Customize and download as PDF.",
};

export default function PrintableCalendarPage() {
  return <PrintableCalendar />;
}
