import TimeChanges from "@/components/TimeChanges";

export const metadata = {
  title: "Time Changes Worldwide | Date & Time Calculator",
  description: "Upcoming daylight saving time changes around the world. Find out when clocks will change in different countries.",
};

export default function TimeChangesPage() {
  return <TimeChanges />;
}
