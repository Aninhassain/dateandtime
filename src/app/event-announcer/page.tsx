import EventTimeAnnouncer from "@/components/EventTimeAnnouncer";

export const metadata = {
  title: "Event Time Announcer | Date & Time Calculator",
  description: "Share event times in multiple time zones. Generate easy-to-share event time announcements.",
};

export default function EventAnnouncerPage() {
  return <EventTimeAnnouncer />;
}
