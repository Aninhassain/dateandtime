import TimeDurationCalculator from "@/components/TimeDurationCalculator";

export const metadata = {
  title: "Time Duration Calculator | Date & Time Calculator",
  description: "Calculate hours, minutes, and seconds between two times. Free online time duration calculator.",
};

export default function TimeDurationPage() {
  return <TimeDurationCalculator />;
}
