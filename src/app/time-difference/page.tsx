import TimeDifference from "@/components/TimeDifference";

export const metadata = {
  title: "Time Difference Calculator | Date & Time Calculator",
  description: "Calculate time difference between any two cities worldwide. Compare current times across time zones.",
};

export default function TimeDifferencePage() {
  return <TimeDifference />;
}
