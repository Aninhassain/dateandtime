import DaysOfWeek from "@/components/DaysOfWeek";

export const metadata = {
  title: "Days of the Week | Date & Time Calculator",
  description: "Learn about the history and meaning behind each day of the week.",
};

export default function DaysOfWeekPage() {
  return <DaysOfWeek />;
}
