import TimezoneMap from "@/components/TimezoneMap";

export const metadata = {
  title: "Time Zone Map | Date & Time Calculator",
  description: "Visual world map with time zones. See current time in all time zones at a glance.",
};

export default function TimezoneMapPage() {
  return <TimezoneMap />;
}
