import EasterCountdown from "@/components/EasterCountdown";

export const metadata = {
  title: "Easter Countdown | Date & Time Calculator",
  description: "Countdown to Easter Sunday. Watch the days, hours, minutes and seconds until Easter.",
};

export default function EasterCountdownPage() {
  return <EasterCountdown />;
}
