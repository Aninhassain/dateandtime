import DateDurationCalculator from "@/components/DateDurationCalculator";

export const metadata = {
  title: "Date Duration Calculator | Date & Time Calculator",
  description: "Calculate the exact duration between two dates in years, months, weeks, and days. Free online date duration calculator.",
};

export default function DateDurationPage() {
  return <DateDurationCalculator />;
}
