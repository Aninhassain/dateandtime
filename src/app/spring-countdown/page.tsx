import SpringCountdown from "@/components/SpringCountdown";

export const metadata = {
  title: "Spring Countdown | Date & Time Calculator",
  description: "Countdown to the first day of spring. Watch the days, hours, minutes and seconds until the spring equinox.",
};

export default function SpringCountdownPage() {
  return <SpringCountdown />;
}
