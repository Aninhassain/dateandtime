import ClockWidget from "@/components/ClockWidget";

export const metadata = {
  title: "Clock Widget for Your Site | Date & Time Calculator",
  description: "Get an embeddable clock widget for your website with customizable options.",
};

export default function ClockWidgetPage() {
  return <ClockWidget />;
}
