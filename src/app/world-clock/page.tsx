import WorldClock from "@/components/WorldClock";

export const metadata = {
  title: "World Clock | Date & Time Calculator",
  description: "View the current time in major cities around the world with real-time updates.",
};

export default function WorldClockPage() {
  return <WorldClock />;
}
