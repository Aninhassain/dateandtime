import DateDifferenceCalculator from "@/components/DateDifferenceCalculator";

export const metadata = {
  title: "Date Difference Calculator | Date & Time Calculator",
  description: "Calculate the exact difference between two dates in days, weeks, months, and years. Free online date difference calculator.",
};

export default function DateDifferencePage() {
  return <DateDifferenceCalculator />;
}
